version: '3.8'

services:
  crm-frontend-qa:
    image: syedus06/crm-frontend:qa
    container_name: crm-frontend-qa
    env_file: ../.env
    environment:
      - NODE_ENV=qa
      - NEXT_PUBLIC_API_BASE_URL=${NEXT_PUBLIC_API_BASE_URL}
      - NEXT_PUBLIC_KEYCLOAK_URL=${NEXT_PUBLIC_KEYCLOAK_URL}
      - NEXT_PUBLIC_CAL_URL=${NEXT_PUBLIC_CAL_URL}
      - NEXT_PUBLIC_CAL_CLIENT_ID=${NEXT_PUBLIC_CAL_CLIENT_ID}
      - CAL_CLIENT_SECRET=${CAL_CLIENT_SECRET}
    ports:
      - "${FRONTEND_PORT:-3010}:3000"
    volumes:
      - ${CRM_BASE:-/home/ubuntu/CRM}/qa/frontend/logs:/app/logs
    networks:
      - qa-network
      - proxy-network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

networks:
  qa-network:
    name: crm-qa-network
    external: true
  proxy-network:
    name: proxy-network
    external: true
