/**
 * DEPRECATED - Use the consolidated types from `features/exams/types` instead
 * 
 * This file re-exports types from the deprecated model directory
 * for backward compatibility during the transition to the new types.
 */

export * from '../types';
