/**
 * UI Context Module
 * 
 * This module exports all context providers and hooks for UI state.
 */

// Export theme context
export * from './ThemeContext';

// Export table view context
export * from './TableViewContext';

// Don't export the context template as it's not meant to be used directly